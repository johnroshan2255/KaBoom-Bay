# Screenshots

Captured automatically with headless Chrome from the running game. Open `index.html` in a browser to view them with captions.

Frames 01–05 are an online match (two humans + bots, phase timing scaled 0.3×). Frames 06–08 are the offline sandbox (`?sandbox&seed=42`). Frames 09–11 show the current renderer: textured voxels, ambient occlusion, heroes and mist.
