# Screenshots

One frame, captured with headless Chrome (1280 × 720, high quality tier) from the offline sandbox
(`?sandbox&seed=7`): a single island as the current build renders it. Compare with
`../reference/art-direction-01.png`, the art target. Open `index.html` to view it with a caption.
